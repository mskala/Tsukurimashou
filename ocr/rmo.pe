#!/usr/local/bin/fontforge

#
# Overlap removal script
#

# This is copied from the current version of the Tsukurimashou project,
# but relicensed as public domain because it's so trivial.

Open($1);
SelectAll();
RemoveOverlap();
Generate($1);
