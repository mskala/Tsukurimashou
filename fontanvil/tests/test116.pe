#!/usr/local/bin/fontanvil
#Needs: fonts/merged.cff

Quit(77) # Needs a missing font file

srcdir=GetEnv("srcdir")
if (srcdir=="")
  srcdir=""
else
  srcdir=srcdir+"/"
endif

Open(srcdir+"tests/fonts/merged.cff")
GetCoverageCounts()
Quit()
